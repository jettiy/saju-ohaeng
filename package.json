{
  "name": "saju-ohaeng-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "사주팔자 - 오행: 사주 입력 → 오행 스탯 JRPG (웹 빌드, OpenAI Game Builders Seoul)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.80.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
