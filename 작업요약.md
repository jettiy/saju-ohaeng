# 사주팔자 오행 — 웹 재빌드 작업 요약

> OpenAI Game Builders Seoul 참전용. Love2D 데스크톱 게임을 **TS/Phaser 웹 게임**으로 재빌드한 기록.
> 마지막 업데이트: 2026-08-09

---

## 🎮 지금 바로 플레이

- **라이브**: https://jettiy.github.io/saju-ohaeng/
- **저장소**: https://github.com/jettiy/saju-ohaeng
- **배포 방식**: `git push` → GitHub Actions 자동 빌드 → Pages 자동 갱신

### 현재 플레이 가능한 흐름 (핵심 훅)
```
타이틀 ─ENTER→ 사주 입력(연/월/일/시 + 양력/음력, 방향키) ─ENTER→ 오행 결과
                                                              ├ 사주팔자(을해 정인 임오 병오 …)
                                              오행 가중치 5개 컬러 막대 (총합 8.0)
                                              강점/약점 오행 + 생업 스탯
```
- 이 흐름은 **라이브 URL과 로컬 모두에서 검증 완료**.
- 오행 총합 = 8.0 (천간 4.0 + 지지 장간 4.0) — 게임의 중심 불변량, 헤드리스·브라우저 교차검증 합격.

---

## 왜 웹으로 다시 만들었나

대회 조건과 기존 프로젝트가 정면 충돌:

| | 기존(Love2D) | 대회 요구 |
|---|---|---|
| 빌드 | 데스크톱 .exe/.love | **브라우저 웹 빌드** |
| 제작 도구 | (기존 AI) | **Codex 사용** |
| 검증 | love.exe 직접 실행 필요(이 셸에선 불가) | 헤드리스 테스트 가능 |

그래서 **설계 문서(20개) + 순수 도메인 로직은 그대로 재사용**하고, 엔진만 Lua→TS, love→Phaser로 갈아끼움. 사주 계산 알고리즘(이 게임의 독창성 핵심)은 이미 완성·테스트된 상태라 1:1 이식.

---

## 무엇을 했나 (영역별)

### 1. 도메인 전량 이식 (Lua → TypeScript) ✅
`src/domain/**` — phaser/DOM 의존성 ZERO. 순수 로직이라 단위 테스트로 검증 가능.
- `saju/`: Ohaeng · BirthDate · FourPillars · OhaengBalance · SajuCalculator (사주팔자 산출)
- `ohaeng/`: ElementInteraction(상생/상극) · StatMapping(오행→스탯)
- `battle/`: Combatant · Skill(DEFS) · Battle(턴제 전투 애그리거트)
- `world/`: FarmTile(농사 사이클)
- `player/`: Stats(불변 값객체) · GameState(세션 상태)
- `relationship/`: Companion(동료)

### 2. Application / Infrastructure ✅
- `application/GameSession.ts`: GameState 보관 + 사주 적용 유스케이스 + 세이브 조율
- `infrastructure/persistence/SaveRepository.ts`: localStorage + JSON 직렬화 (주입 가능 → 테스트 용이)

### 3. Phaser 웹 빌드 ✅
- `presentation/scenes/`: Boot · Title · SajuInput(핵심 훅) · SajuResult(오행 막대)
- GameSession은 레지스트리로 씬 간 공유
- 한국어 렌더링: Noto Sans KR 웹폰트

### 4. 배포 파이프라인 ✅
- Vite 정적 빌드 → GitHub Actions → GitHub Pages 자동 배포
- `vite.config.ts`: 프로덕션 base `/saju-ohaeng/` 고정

### 5. Codex 핸드오프 ✅
- `CODEX_HANDOFF.md`: 남은 씬(Field/Battle/Companion/Ending) 구현 명세 + 도메인 API + 제약

---

## 검증 증거 (숫자)

| 항목 | 결과 |
|---|---|
| 단위 테스트 | **126 passed** (`npx vitest run`) |
| 타입체크 | **0 에러** (`npx tsc --noEmit`) |
| 프로덕션 빌드 | 성공, gzip **347KB** |
| 라이브 동작 | 사주 플로우 정상 (오행 총합 8.0, 사주팔자 산출 확인) |
| 도메인 교차검증 | 헤드리스 결과 == 브라우저 결과 (동일 TS 도메인) |

---

## 해결한 환경 이슈 (★ 반드시 기억)

프로젝트 경로에 **한글(`사주스타듀밸리`)**이 있어 JS 도구 3종이 깨짐. 아래 우회책 적용됨:

1. **pnpm 사용 금지** → 한글 경로에서 junction 생성 실패. **npm만 사용**.
2. **Vite `emptyOutDir: false`** → 기존 dist 비우는 단계에서 크래시. 비활성화. (배포 전 수동 clean: `cmd //c "rmdir /s /q dist"`)
3. **GitHub Pages base `/saju-ohaeng/`** → Vite `base:'./'`가 HTML script src를 상대경로로 안 만들어 프로젝트 Pages에서 404. 프로덕션 base 고정으로 해결.

---

## 프로젝트 구조

```
web/                              ← 이게 웹 게임 프로젝트 (자체 git repo)
├── src/
│   ├── domain/                   순수 TS (phaser 의존 ZERO, vitest 로 검증)
│   ├── application/              GameSession
│   ├── infrastructure/           SaveRepository (localStorage)
│   └── presentation/             sceneKeys + scenes/* (Phaser)
├── tests/                        vitest 스펙 6종 (126 테스트)
├── public/assets/game_art/       Cozy Farm 에셋 넣는 곳 (README 참조)
├── .github/workflows/deploy.yml  GitHub Pages 자동 배포
├── CODEX_HANDOFF.md              Codex 구현 명세
├── 작업요약.md                    ← 이 파일
└── (package.json, tsconfig, vite/vitest config)
```

부모 폴더(`사주스타듀밸리/`)의 원본 Love2D 코드·설계 문서(`docs/`, `ASSET_SETUP.md`)는 **참조용**으로 남아있음.

---

## 다음에 할 것

### 네가 할 것 (2가지)
1. **Cozy Farm 다운로드** → `web/public/assets/game_art/` 에 PNG 분류
   - https://shubibubi.itch.io/cozy-farm (무료) + 권장 https://shubibubi.itch.io/cozy-people
   - 상세: `web/public/assets/game_art/README.txt`
2. **Codex 구동** → `web/` 디렉토리에서 `CODEX_HANDOFF.md` 따라 나머지 씬 구현
   - 우선순위: **FieldScene**(탐험+농사) → **BattleScene**(턴제 전투) → CompanionScene → EndingScene
   - 도메인(전투/농사/오행)은 이미 완성·테스트됨 → Codex는 UI/씬만 붙이면 됨

### 분업
- **omp(나)**: 아키텍처 + 도메인 이식 + App/Infra + 씬 패턴 + 배포 + Codex 명세 ✅ 완료
- **Codex**: 나머지 씬 피처 (명세 준비됨)
- **omp 지원 가능**: QA·버그수정·리팩터·도메인 확장·빌드 최적화 (K-STONKS 때처럼)

---

## 명령어 치트시트 (web/ 에서)

```bash
npm install            # 의존성 (★ npm만, ptrain 금지)
npx vitest run         # 도메인 단위 테스트 (126개)
npx tsc --noEmit       # 타입체크
npx vite build         # 정적 빌드 → dist/
npx vite dev           # 개발 서버 (HMR, localhost:5173)
git add -A && git commit -m "..." && git push   # 푸시하면 자동 배포
```

---

## 핵심 파일 빠른 참조

| 파일 | 역할 |
|---|---|
| `src/domain/saju/SajuCalculator.ts` | 사주 계산 알고리즘 (오행 총합 8.0 보장) |
| `src/domain/battle/Battle.ts` | 턴제 전투 로직 (데미지/상성/승패) |
| `src/application/GameSession.ts` | 게임 세션 조율 (사주 적용/세이브) |
| `src/presentation/scenes/SajuInputScene.ts` | 핵심 훅 씬 (사주 입력 패턴 = 다른 씬의 정답 예시) |
| `CODEX_HANDOFF.md` | Codex용 구현 명세 |
| `.github/workflows/deploy.yml` | 자동 배포 |
